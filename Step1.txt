现在开始编程，第一个微服务：监听微服务， 监听三条链。然后将消息写入rabbitmq（在docker中，你应该能操作我电脑里面的docker desktop吧），接下来是测试（和我们的规划是不同的，仅仅是测试），rabbitmq原样输出给同一个索引微服务（这个”所谓的索引“微服务（不是真正的微服务）只有一个print，只有一个功能，就是输出，输出，原样输出rabbitmq发送给他的消息）。   实现上述目标后，写一个阶段文档。 然后结束，先不要往下走。