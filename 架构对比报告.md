# MultiIndexer vs epusdt 架构对比报告

## 执行摘要

**结论：epusdt 的架构在几乎所有维度上都优于 MultiIndexer。建议以 epusdt 的设计模式为标准重构。**

epusdt 是一个生产级的多链支付网关，已经解决了 MultiIndexer 当前面临的几乎所有架构问题。
两者做的是同一件事（多链监听 + 事件处理），但工程成熟度差距巨大。

---

## 一、项目定位对比

| 维度 | MultiIndexer | epusdt (GM Pay) |
|------|-------------|-----------------|
| 定位 | 多链事件监听索引器 | 多链多币种支付网关 |
| 语言 | Go | Go |
| 链支持 | ETH, BSC, Solana (3条) | ETH, BSC, Polygon, Plasma, TRON, Solana (6条) |
| 状态 | 开发/学习阶段 | 生产级开源项目 |
| 前端 | 无（fake-api） | Vue.js SPA（内嵌） |
| 数据库 | MySQL（硬编码） | SQLite / MySQL / PostgreSQL（可选） |

---

## 二、核心架构差异

### 2.1 链/代币配置方式（最关键的区别）

**MultiIndexer — 硬编码**
```go
// listener/main.go — 链、RPC、钱包全部写死在代码里
EVMWatcher{chain: "eth", rpcURL: getEnv("ETH_RPC_URL", "..."), interval: 12 * time.Second}
EVMWatcher{chain: "bsc", rpcURL: getEnv("BSC_RPC_URL", "..."), interval: 3 * time.Second}
SolWatcher{rpcURL: getEnv("SOL_RPC_URL", "..."), wallet: getEnv("SOL_WALLET", "...")}

// listener/evm.go — 代币列表写死
func lookupToken(chain, contract string) (string, int) {
    known := map[string]map[string]struct{...}{
        "eth": {"0xdac17f958d2ee523a2206206994597c13d831ec7": {"USDT", 6}},
        // 每加一个代币都要改代码、重新编译
    }
}
```

**epusdt — 数据库驱动 + Admin API 热配置**
```
chains 表 → 管理所有链（network, enabled, min_confirmations, scan_interval_sec）
chain_tokens 表 → 管理所有代币（network, symbol, contract_address, decimals, enabled）
rpc_nodes 表 → 管理所有 RPC 节点（network, url, type, weight, enabled, status）
```

加一条新链 = 在 Admin 页面点几下，零代码修改。加一个新代币 = 填写合约地址和精度，零代码修改。

**差距评估：这是最根本的架构差异。MultiIndexer 每加一条链/代币都需要改代码重新编译，epusdt 完全不需要。**

---

### 2.2 链监听模式

**MultiIndexer — 轮询（Polling）**
```go
// listener/evm.go — 每 N 秒调一次 eth_getLogs
ticker := time.NewTicker(w.Interval)
for range ticker.C {
    logs, err := client.FilterLogs(ctx, ethereum.FilterQuery{
        FromBlock: big.NewInt(lastBlock),
        ToBlock:   big.NewInt(lastBlock + 50),
        Topics:    [][]common.Hash{{transferTopic, approvalTopic, ...}},
    })
}
```

- 每 12 秒（ETH）/ 3 秒（BSC）/ 1 秒（Solana）发起一次 RPC 调用
- 浪费 RPC 配额，增加延迟
- 公共 RPC 有严格限速，容易被封

**epusdt — WebSocket 订阅（EVM 链）+ 轮询（TRON/Solana）**
```go
// src/task/listen_evm_ws.go — 实时订阅 Transfer 事件
func runEvmWsLogListener(ctx, network, recipient, client, contracts) {
    query := ethereum.FilterQuery{Addresses: contractAddresses}
    sub, err := client.SubscribeFilterLogs(ctx, query, logChan, nil)
    for {
        select {
        case err := <-sub.Err():  // 断线自动重连
            // 指数退避：2s → 4s → 8s → ... → 60s
        case vLog := <-logChan:   // 实时收到事件
            TryProcessEvmERC20Transfer(vLog, ...)
        }
    }
}
```

- EVM 链用 WebSocket 实时推送，延迟 < 1 秒
- 内置断线重连 + 指数退避（2s → 60s）
- TRON 用块扫描（每 3 秒），Solana 用签名轮询（每 5 秒）

**差距评估：WebSocket 是生产级方案，轮询是开发级方案。**

---

### 2.3 EVM 链抽象

**MultiIndexer — 无抽象，两个独立的 Watcher**
```
EVMWatcher (listener/evm.go) — 用于 ETH、BSC
SolWatcher (listener/sol.go) — 用于 Solana
```
- EVMWatcher 和 SolWatcher 没有共同接口
- 但至少 EVM 链共用了一个 struct

**epusdt — 高度抽象的共享基础设施**
```
runEvmWsLogListener()  ← 所有 EVM 链共用这一个函数
├── listen_eth.go      ← 只定义 network 名 + recipient 类型（~30 行）
├── listen_bsc.go      ← 同上
├── listen_polygon.go  ← 同上
└── listen_plasma.go   ← 同上

TryProcessEvmERC20Transfer() ← 所有 EVM 链的转账处理共用
```

新增一个 EVM 链（如 Arbitrum）只需要 ~130 行样板代码，复用所有核心逻辑。

---

### 2.4 动态配置热加载

**MultiIndexer — 无**
配置写死在环境变量里，改配置 = 重启服务。

**epusdt — Chain Watchdog 模式**
```go
// src/task/listen_chain_common.go
func chainEnabledWatchdog(ctx, network, interval) {
    ticker := time.NewTicker(10 * time.Second)
    for range ticker.C {
        chain := data.GetChainByNetwork(network)
        if !chain.Enabled || fingerprintChanged(chain) {
            cancel()  // 触发监听器重启
            // 新的 context 会加载最新的链配置
        }
    }
}
```

- 每 10 秒检查链是否被禁用
- 代币合约列表变化时自动重启监听器
- 钱包地址每 5 秒刷新一次
- **零停机配置更新**

---

### 2.5 RPC 节点管理

**MultiIndexer — 单节点，无容错**
```go
client, err := ethclient.Dial(getEnv("ETH_RPC_URL", "https://..."))
// 一个节点挂了就挂了
```

**epusdt — 多节点 + 健康检查 + 权重选择**
```
rpc_nodes 表：
  network | url                          | type | weight | status | last_latency_ms
  eth     | wss://eth-mainnet.g.alchemy  | ws   | 100    | 1      | 45ms
  eth     | wss://eth.llamarpc.com       | ws   | 80     | 1      | 120ms
  eth     | https://eth.drpc.org         | http | 50     | 1      | 200ms

rpc_health_job.go — 每 60 秒 TCP 健康检查
```

- 多节点权重轮询
- 自动故障转移
- 延迟监控
- Admin API 动态增删节点

---

### 2.6 数据存储架构

**MultiIndexer — 单一 MySQL**
```
MySQL (indexer 数据库)
├── events      — 所有链上事件
├── alerts      — 告警
├── blocks      — 区块头
└── indexer_state — 同步进度
```

**epusdt — 双数据库架构**
```
主数据库 (MySQL/PostgreSQL/SQLite 可选)
├── orders              — 支付订单
├── wallet_address      — 收款钱包
├── chains              — 链配置
├── chain_tokens        — 代币配置
├── rpc_nodes           — RPC 节点
├── api_keys            — 商户 API 密钥
├── settings            — 系统设置
├── admin_users         — 管理员
├── notification_channels — 通知渠道
└── provider_orders     — 第三方订单

运行时数据库 (始终 SQLite)
└── transaction_lock    — 金额锁（高频读写的临时数据）
```

关键设计：将高频、临时性的 `transaction_lock` 表分离到 SQLite，
避免锁竞争影响主数据库的业务查询。

---

### 2.7 消息队列 vs 直接处理

**MultiIndexer — RabbitMQ 解耦**
```
Listener → RabbitMQ → Processor → MySQL
```
- 优点：解耦、可扩展
- 缺点：引入 RabbitMQ 依赖，增加运维复杂度
- 问题：消息积压（曾出现 35 万条未处理消息）

**epusdt — 直接处理 + 数据库轮询**
```
Listener → 直接调用 TryProcessEvmERC20Transfer() → 写数据库
```
- 无消息队列依赖
- 更简单，延迟更低
- 后台任务（过期订单、回调重试）用 `robfig/cron` 定时轮询数据库

**差距评估：对于支付场景，epusdt 的直接处理模式更合适。
MultiIndexer 的 MQ 模式适合需要高吞吐的索引场景，但当前实现过于复杂。**

---

### 2.8 支付匹配机制（epusdt 独有）

**TransactionLock — 金额锁定算法**
```
1. 商户创建订单 → 系统分配 (钱包地址 + 精确金额)
2. 写入 transaction_lock 表：(network, address, token, amount_scaled)
3. 链上收到转账 → 查 transaction_lock 找到匹配的订单
4. 匹配成功 → 订单标记已支付 → 删除 lock
5. 超时 → 自动过期 → 释放 lock
```

唯一约束 `(network, address, token, amount_scaled, amount_precision)` 确保：
同一地址、同一代币、同一金额只能有一个待支付订单。

**MultiIndexer 没有任何等价机制。**

---

### 2.9 通知系统

**MultiIndexer — 硬编码告警**
```go
// processor/alert.go — 硬编码价格表
var tokenPricesUSD = map[string]float64{
    "USDT": 1.0, "USDC": 1.0, "WETH": 3500.0, "WBTC": 65000.0,
}
// 只能写入 MySQL 和 Redis，没有外部通知
```

**epusdt — 插件化通知系统**
```go
// notify/dispatcher.go
var registry = map[string]Sender{}

func RegisterSender(channelType string, sender Sender) {
    registry[channelType] = sender
}

func Dispatch(event, text) {
    for _, channel := range enabledChannels {
        registry[channel.Type](channel.Config, text)
    }
}
```

- 已实现：Telegram 通知
- 可扩展：Webhook、Email、Discord（只需实现 Sender 接口）
- 事件类型：支付成功、订单过期、每日报表
- Admin API 配置通知渠道

---

### 2.10 管理后台

**MultiIndexer — 无**
只有 `fake-api` 提供基础的只读查询 API。

**epusdt — 完整的 Admin 管理系统**
- Vue.js SPA 嵌入式前端
- JWT 认证
- CRUD：链、代币、RPC 节点、钱包、API 密钥、订单、通知渠道
- 安装向导（首次运行引导配置）
- 仪表盘统计

---

## 三、详细对比表

| 维度 | MultiIndexer | epusdt | 优胜 |
|------|-------------|--------|------|
| 链配置 | 硬编码在 Go 代码 | 数据库 + Admin API | epusdt |
| 代币配置 | 硬编码在 Go 代码 | 数据库 + Admin API | epusdt |
| 监听方式 | 全部轮询 | EVM 用 WebSocket，其余轮询 | epusdt |
| RPC 管理 | 单节点，无容错 | 多节点 + 健康检查 + 权重 | epusdt |
| 链抽象 | 无共同接口 | 共享 `runEvmWsLogListener()` | epusdt |
| 动态重载 | 无（需重启） | Watchdog 自动重载 | epusdt |
| 数据库 | 单一 MySQL | 双数据库（主 + 运行时 SQLite） | epusdt |
| ORM | 原生 SQL | GORM（迁移、种子数据） | epusdt |
| 配置管理 | 环境变量 | .env + 数据库设置表 + Admin API | epusdt |
| 消息队列 | RabbitMQ | 无（直接处理） | 看场景 |
| 告警/通知 | 硬编码，仅写 DB | 插件化，支持 Telegram/Webhook | epusdt |
| 管理后台 | 无 | Vue.js SPA + JWT | epusdt |
| 前端 | 无 | 内嵌 Vue.js | epusdt |
| CLI | `go run .` | Cobra 命令行框架 | epusdt |
| 日志 | `fmt.Println` | Zap + 日志轮转 | epusdt |
| 错误恢复 | defer+recover | defer+recover + 指数退避重连 | epusdt |
| 部署 | 手动编译 | Docker + 安装向导 | epusdt |
| 测试 | 无 | 有 testutil | epusdt |
| 文档 | 无 | Wiki + DEVELOPMENT_STANDARD.md | epusdt |

---

## 四、MultiIndexer 的独有优势

公平起见，MultiIndexer 也有几个 epusdt 不具备的特点：

1. **全量事件索引** — MultiIndexer 记录所有 Transfer/Mint/Burn/Approval 事件；
   epusdt 只关心支付相关的转账，不索引所有链上事件。

2. **告警系统** — 大额转账检测 + 监控地址告警，epusdt 没有这个功能。

3. **RabbitMQ 解耦** — 如果未来需要将事件分发给多个消费者（如同时写入数据库、
   觨发 Webhook、推送 WebSocket），MQ 架构更有优势。

4. **Redis 热数据缓存** — 最新事件、统计信息缓存在 Redis 中，查询更快。

**但这些优势可以通过在 epusdt 架构上扩展来获得，而 epusdt 的架构优势无法通过
修补 MultiIndexer 来获得。**

---

## 五、重构建议

### 方案 A：以 epusdt 为蓝本完全重写（推荐）

将 epusdt 的架构模式移植到 MultiIndexer：

1. **数据库驱动的链/代币配置** — 创建 `chains`、`chain_tokens`、`rpc_nodes` 表
2. **Admin REST API** — 用 Echo 或 Gin 搭建管理后台
3. **EVM WebSocket 监听** — 替换当前的轮询模式
4. **共享 EVM 基础设施** — `runEvmWsLogListener()` 统一处理所有 EVM 链
5. **Chain Watchdog** — 实现动态配置热加载
6. **RPC 节点管理** — 多节点 + 健康检查 + 故障转移
7. **GORM ORM** — 替换原生 SQL，支持多数据库
8. **保留独有功能** — 全量事件索引、告警系统、Redis 缓存

### 方案 B：渐进式改造

保留 MultiIndexer 现有代码，逐步引入 epusdt 的模式：

1. 先把硬编码的链/代币配置迁移到数据库
2. 再加 Admin API
3. 再切换到 WebSocket
4. 逐步重构其他模块

### 方案 C：直接基于 epusdt 二次开发

Fork epusdt，在其基础上添加 MultiIndexer 的独有功能（全量索引、告警、MQ）。

---

## 六、结论

**epusdt 在架构设计上全面领先 MultiIndexer。** 核心差距在于：

1. **配置驱动 vs 代码驱动** — epusdt 用数据库管理链和代币，MultiIndexer 硬编码
2. **WebSocket vs 轮询** — epusdt 实时监听，MultiIndexer 定时轮询
3. **抽象 vs 耦合** — epusdt 的 EVM 链高度抽象，MultiIndexer 无抽象
4. **生产级 vs 学习级** — epusdt 有 RPC 容错、热重载、管理后台、日志系统

**建议采用方案 A（完全重写），以 epusdt 的架构为骨架，保留 MultiIndexer 的
全量索引和告警能力。这将是一个质的飞跃。**
